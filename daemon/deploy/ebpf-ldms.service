[Unit]
Description = EBPF to LDMS streaming daemon
After = nersc-ldmsd.sampler.service
BindsTo = nersc-ldmsd.sampler.service
After = munge.service
BindsTo = munge.service

[Service]
Type = simple
EnvironmentFile = /etc/sysconfig/ebpf_ldms
Environment="LD_LIBRARY_PATH=/opt/ovis-ldms/lib:$LD_LIBRARY_PATH"
ExecStartPre=bash -c "until /opt/ovis-ldms/sbin/ldms_ls -a munge -h ${EBPF_LDMS_LDMSD_HOST} -p ${EBPF_LDMS_LDMSD_PORT} > /dev/null; do sleep 10; done"
ExecStartPre = bpftool map create /sys/fs/bpf/LDMS_SHARED_STREAM type ringbuf entries ${EBPF_LDMS_RING_SIZE} name LDMS_SHARED_STREAM
ExecStart = /usr/sbin/ebpf_ldms \
                --host ${EBPF_LDMS_LDMSD_HOST} \
                --port ${EBPF_LDMS_LDMSD_PORT} \
                --stream ${EBPF_LDMS_LDMSD_STREAM} \
                --msglimit ${EBPF_LDMS_MSGLIMIT} \
                --interval ${EBPF_LDMS_INTERVAL} \
                --authentication ${EBPF_LDMS_LDMSD_AUTH} \
                --hostname %H
ExecStopPost = rm /sys/fs/bpf/LDMS_SHARED_STREAM
TimeoutStartSec=infinity

[Install]
WantedBy = nersc-ready.target
