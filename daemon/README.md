# ebpf_ldms — daemon

This directory contains the `ebpf_ldms` daemon crate and the `ldms_stream` FFI bindings.

See the [top-level README](../README.md) for project overview, build instructions, and configuration.
