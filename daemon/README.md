# ebpf_streamer — daemon

This directory contains the `ebpf_streamer` daemon crate and the `ldms_stream` FFI bindings.

See the [top-level README](../README.md) for project overview, build instructions, and configuration.
